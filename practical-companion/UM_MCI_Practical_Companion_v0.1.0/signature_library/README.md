# Signature library

This library contains **starter recognizer seeds** for edge-family instances. These are GitHub-level practical objects rather than stable core ontology objects.

## Directory layout

- `starter_seeds/` — maintainer-provided starter signature seeds.
- `contributed_seeds/` — community signature seeds under review.
- `templates/` — reusable templates.

The workspace validator scans both starter and contributed seeds.
