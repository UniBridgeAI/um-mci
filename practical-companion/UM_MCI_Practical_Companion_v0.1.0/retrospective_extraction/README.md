# Retrospective extraction

This directory supports reconstruction of UM-MCI traces from past tasks, papers, or execution logs.

Use cases:
- testing whether the current ontology achieves near coverage,
- accumulating route experience across domains,
- checking whether dimensions, artifacts, validators, tensions, and contexts remain adequate,
- turning historical success and failure into reusable meta-control data.
